packages:
  yum:
    mysql-devel: []

commands:
  install_mysqlclient:
    command: |
      # Update the package list
      yum update -y
      # Install the MySQL development libraries
      yum install -y mysql-devel
